{
  "name": "@mono/common-util",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mono/common-dto": "workspace:*",
    "lodash.intersection": "^4.4.0"
  },
  "devDependencies": {
    "@types/lodash.intersection": "^4.4.9",
    "typescript": "^5.8.3",
    "vitest": "^3.2.6"
  }
}
